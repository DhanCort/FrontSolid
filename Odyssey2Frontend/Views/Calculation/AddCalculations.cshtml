@model List<AttributeModel>

@{
	ViewData["Title"] = "Calculation";

	Dictionary<int, List<CalculationModel>> dicattrcal = ViewBag.dicattrcal;
}
<div class="row">
	<div class="col-sm-8">
		<h2>Calculation - @ViewBag.strTypeId</h2>
	</div>
	<div class="col-sm-4">
		<a type="button" class="btn btn-info btn-sm float-right" id="back-workflow-job" href="/Jobs" hidden>
			<i class="fa fa-angle-left" aria-hidden="true"></i> Back
		</a>
	</div>
</div>
<div class="row">
	<div class="col-md-12" id="addCalculationsBody">

		<!--<ul class="nav nav-pills">
			<li class="nav-item"><a class="nav-link active" data-toggle="tab" href="#ByProduct"> By Product</a></li>-->
			@*<li class="nav-item"><a class="nav-link" data-toggle="tab" href="#BySpect">By Intent</a></li>
			<li class="nav-item"><a class="nav-link" data-toggle="tab" href="#ByProcess">By Process</a></li>
			<li class="nav-item"><a class="nav-link" data-toggle="tab" id="ByResourcesPill" href="#ByResources">By Resources</a></li>*@
		<!--</ul>-->

		<div class="tab-content">
			<div class="tab-pane fade show active" id="ByProduct">
				<div class="">
					<div class="col-md-12">
						@{ await Html.RenderPartialAsync("ByProductPartialView", new CalculationModel
						 {
							 strPrintshopId = ViewBag.strPrintshopId,
							 intnPkProduct = ViewBag.intProductPk
						 });
						}
					</div>
				</div>
			</div>
			@*
			<div class="tab-pane fade panel-group" id="BySpect">
				<div class="">
					<div class="col-md-12">
						<h3>By Intent</h3>
						@{
							Html.RenderPartial("ByIntentPartialView", new CalculationModel
							{
								darrattr = ViewBag.darrattr,
								darrIntent = ViewBag.Intent,
								intnPkProduct = ViewBag.intProductPk
							});
						}
					</div>
				</div>
			</div>
			<div id="ByProcess" class="tab-pane fade panel-group">
				<div class="">
					<div class="col-md-12">
						@{ Html.RenderPartial("ByProcessPartialView", new CalculationModel
							{
								strPrintshopId = ViewBag.strPrintshopId,
								intnPkProduct = ViewBag.intProductPk,
							});
						}
					</div>
				</div>
			</div>
			<div id="ByResources" class="tab-pane fade panel-group">
				<div class="">
					<div class="col-md-12">
						@{ Html.RenderPartial("ByResourcePartialView", new CalculationModel
							{
								strPrintshopId = ViewBag.strPrintshopId,
								darrattr = ViewBag.darrattr,
								intnPkProduct = ViewBag.intProductPk,
							});
						}
					</div>
				</div>
			</div>
			*@
		</div>
	</div>
</div>

<!-- Modal -->
<div id="conditionToApplyModal" class="modal fade" tabindex="-1" role="dialog">
	<div class="modal-dialog modal-dialog-centered modal-xl">
		<div class="modal-content">
			<div class="modal-header">
				<h4 class="modal-title">Conditions To Apply</h4>
				<button type="button" class="close" data-dismiss="modal"><span>&times;</span></button>
			</div>
			<div class="modal-body" id="conditionToApply">

			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-primary btnSaveConditionToApply"><i class="fa fa-save"></i> Save Condition</button>
				<button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
			</div>
		</div>

	</div>
</div>

@section Scripts {
	@{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}